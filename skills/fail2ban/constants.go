// Package fail2ban provides playbooks for managing the fail2ban intrusion prevention system.
// Fail2ban monitors log files for suspicious activity and automatically bans IPs
// that show malicious patterns like brute-force login attempts.
package fail2ban

// No argument constants needed for fail2ban playbooks.
// Fail2ban playbooks operate without additional arguments.
